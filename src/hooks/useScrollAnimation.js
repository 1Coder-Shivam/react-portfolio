// This hook is now replaced by Framer Motion's built-in animation system
// Keeping file for backward compatibility but not using IntersectionObserver anymore
export const useScrollAnimation = () => {
  // Framer Motion handles all scroll animations now
  return null;
};
